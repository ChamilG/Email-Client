import java.io.IOException;
/*
public class Personal implements Recip_ients {
    String name,nickname,email,birthday;
    Personal(String name, String nickname, String email, String birthday) throws IOException {
        this.name = name;
        this.nickname = nickname;
        this.email = email;
        this.birthday = birthday;
       // writeToFile();
    }
   /*
   * @Override
    public void writeToFile() throws IOException {
        String detail = String.format("Personal : %s,%s,%s,%s", name,nickname,email,birthday);
        Filewrite.writeToFile("clientList.txt",detail);
    }
}*/
